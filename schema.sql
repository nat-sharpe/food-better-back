CREATE TABLE items (
  id INTEGER,
  allowed VARCHAR,
  title VARCHAR,
  brand VARCHAR,
  imageURL VARCHAR,
  carbs INTEGER,
  calories INTEGER,
  organic VARCHAR,
  vegan VARCHAR,
  glutenFree VARCHAR
);